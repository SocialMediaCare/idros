# macOS
.DS_Store

# Claude Code
.claude/
settings.local.json

# Dipendenze / build (per il futuro backend)
node_modules/
.next/
dist/
.vercel/

# Variabili d'ambiente (Supabase, Resend, ecc.)
# L'eccezione va tenuta per ultima: in .gitignore vince l'ultima regola
# che corrisponde, quindi un `.env*` più in basso rimetterebbe fuori
# anche il file di esempio.
.env
.env.*
!.env.example
